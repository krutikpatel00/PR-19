import React from 'react';
import './Loading.css'
const Loading = () => {
      return (
            <div className="loading">
                  <div className="spinner"></div>
                  <h2>Loading...</h2>
            </div>
      );
};

export default Loading;